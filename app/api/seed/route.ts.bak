백엔드 더미데이터 생성 시 BankAccount에 bank_name을 직접 저장하고 있지만,
BankAccount 엔티티는 bank_id로 Bank 엔티티와 관계를 맺고 있습니다.

은행이름이 제대로 조회되려면 Bank 테이블에 은행을 먼저 저장하고,
BankAccount에 bank_id를 연결해야 합니다.

아래와 같이 더미데이터 생성 로직을 수정해야 합니다:
1. Bank 테이블에 은행명 리스트를 저장
2. BankAccount 생성 시 bank_id에 해당 은행의 id를 연결

이렇게 하면 프론트에서 정상적으로 은행이름이 조회됩니다.
